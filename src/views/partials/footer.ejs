<footer class="footer mt-auto py-3 bg-light">
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <p class="mb-0 text-muted">© <%= new Date().getFullYear() %> Tahir Heart Institute. All rights reserved.</p>
            </div>
            <div class="col-md-6 text-end">
                <% if (locals.hospitalSettings) { %>
                    <p class="mb-0 text-muted">
                        <i class="fas fa-phone"></i> <%= hospitalSettings.phone %> |
                        <i class="fas fa-envelope"></i> <%= hospitalSettings.email %>
                    </p>
                <% } %>
            </div>
        </div>
    </div>
</footer>